import React from 'react';
import { View, Text } from 'react-native';

export const CalendarScreen = () => {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text className="text-lg font-semibold">캘린더 화면</Text>
    </View>
  );
};